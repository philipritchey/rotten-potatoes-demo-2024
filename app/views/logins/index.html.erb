<h1>Login</h1>

<%= form_with url: "/login", method: :post do |form| %>
    <div>
        <%= form.label :email, style: "display: block" %>
        <%= form.select :email, @emails %>
    </div>

    <div>
        <%= form.submit "Login" %>
    </div>
<% end %>